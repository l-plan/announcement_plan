<%= form_for(@role_source) do |f| %>
  <% if @role_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_source.errors.count, "error") %> prohibited this role_source from being saved:</h2>

      <ul>
      <% @role_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :naam %><br>
    <%= f.text_field :naam %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
